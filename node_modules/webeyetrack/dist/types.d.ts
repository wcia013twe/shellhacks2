import { NormalizedLandmark, Matrix, Classifications } from "@mediapipe/tasks-vision";
export type Point = number[];
export declare enum TrackingStatus {
    FAILED = 0,
    SUCCESS = 1
}
export interface GazeResult {
    facialLandmarks: NormalizedLandmark[];
    faceRt: Matrix;
    faceBlendshapes: Classifications[];
    eyePatch: ImageData;
    headVector: Array<number>;
    faceOrigin3D: Array<number>;
    metric_transform: Matrix;
    gazeState: 'open' | 'closed';
    normPog: Array<number>;
    durations: Record<string, number>;
    timestamp: number;
}
