export default class WebcamClient {
    private videoElement;
    private stream?;
    private frameCallback?;
    constructor(videoElementId: string);
    startWebcam(frameCallback?: (frame: ImageData, timestamp: number) => Promise<void>): Promise<void>;
    stopWebcam(): void;
    private _processFrames;
}
